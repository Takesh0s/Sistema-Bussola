# =============================================================================
#  .gitignore — Sistema Bússola
# =============================================================================

# ── Script de deploy ──────────────────────────────────────────────────────────
commits.sh

# ── Credenciais (NUNCA commitar) ──────────────────────────────────────────────
.env
.env.local
.env.development
.env.production
.env.*.local

# ── Dependências de desenvolvimento ───────────────────────────────────────────
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# ── Supabase Agent Skills (ferramentas locais de IA, não fazem parte da app) ──
.agents/
skills-lock.json

# ── Arquivos gerados pelo Supabase SSR (Next.js — não usamos Next aqui) ───────
utils/

# ── Build/cache ────────────────────────────────────────────────────────────────
dist/
build/
.cache/
.parcel-cache/
.vite/

# ── OS ─────────────────────────────────────────────────────────────────────────
.DS_Store
._*
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
*~

# ── Editores ───────────────────────────────────────────────────────────────────
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo

# ── Logs ───────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── Temporários ────────────────────────────────────────────────────────────────
*.tmp
*.bak
*.orig
*.zip
*.tar.gz
